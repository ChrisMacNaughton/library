<h2>Edit book</h2>

<%= render "form.html", changeset: @changeset,
                        action: book_path(@conn, :update, @book),
                        authors: @authors %>

<%= link "Back", to: book_path(@conn, :index) %>
